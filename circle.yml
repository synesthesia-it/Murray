machine:
  xcode:
    version: 10.0
  environment:
    LANG: en_US.UTF-8
dependencies:
  override:
    - bin/bootstrap-if-needed
  cache_directories:
    - "Carthage"
test:
  override:
  - set -o pipefail
  - swift test
