import Foundation
import Quick
import Nimble
import Files

@testable import MurrayKit

class BoneSpecSpec: QuickSpec {
    override func spec() {
        context("a BoneSpec object") {
            describe("created with default parameters") {
                it("should not error") {
                    
                }
            }
        }
    }
}
